package com.acme.profile;

/** Distractor service that saves display-name profile data, not authentication tokens. */
public final class ProfileService {
    public void saveProfile(String userId, String displayName) {}
}
