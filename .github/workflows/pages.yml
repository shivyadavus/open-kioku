name: Deploy Demo to GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Copy proof docs to demo
        run: |
          cp docs/usefulness-proof.md demo/usefulness-proof.md
          cp docs/large-repo-proof.md demo/large-repo-proof.md
          cp docs/large-java-validation-3.0.4.md demo/large-java-validation-3.0.4.md
          cp docs/large-java-validation-2026-08-31.md demo/large-java-validation-2026-08-31.md
          cp docs/proof.md demo/proof.md
      - name: Setup Pages
        uses: actions/configure-pages@v6
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: './demo'
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
        with:
          timeout: 600000
