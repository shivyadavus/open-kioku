name: RI3 exact SCIP references

on:
  push:
    branches:
      - agent/ri3-resolution-foundation

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/ri3-resolution-foundation
          fetch-depth: 0

      - name: Apply exact-reference patch
        run: |
          python3 -m py_compile scripts/ri3_exact_references_tmp.py
          python3 scripts/ri3_exact_references_tmp.py

      - name: Validate reference trust boundary
        run: |
          cargo fmt --all -- --check
          cargo clippy -p open-kioku-core -p open-kioku-scip -p open-kioku-ingest -p open-kioku-graph --all-targets -- -D warnings
          cargo test -p open-kioku-core -p open-kioku-scip -p open-kioku-ingest -p open-kioku-graph

      - name: Commit validated exact references
        run: |
          git config user.name "open-kioku automation"
          git config user.email "actions@users.noreply.github.com"
          git checkout -- Cargo.lock
          rm -rf scripts/__pycache__
          git rm -f .github/workflows/ri3-exact-references.yml scripts/ri3_exact_references_tmp.py
          git add \
            crates/open-kioku-core/src/lib.rs \
            crates/open-kioku-scip/src/lib.rs \
            crates/open-kioku-ingest/src/lib.rs \
            crates/open-kioku-graph/src/lib.rs \
            crates/open-kioku-graph/src/buffer.rs
          if ! git diff --quiet; then
            echo "unexpected unstaged tracked changes after validation cleanup:" >&2
            git status --short >&2
            exit 1
          fi
          if [ -n "$(git ls-files --others --exclude-standard)" ]; then
            echo "unexpected untracked files after validation cleanup:" >&2
            git ls-files --others --exclude-standard >&2
            exit 1
          fi
          git commit -m "ri3: authorize exact SCIP reference occurrences"
          git pull --rebase origin agent/ri3-resolution-foundation
          git push origin HEAD:agent/ri3-resolution-foundation
