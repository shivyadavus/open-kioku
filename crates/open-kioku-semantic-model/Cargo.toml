[package]
name = "open-kioku-semantic-model"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Shared semantic-model types and contracts for Open Kioku local code intelligence"

[dependencies]
open-kioku-core = { version = "3.0.4", path = "../open-kioku-core" }
serde = { workspace = true, features = ["derive"] }
schemars = { workspace = true }
